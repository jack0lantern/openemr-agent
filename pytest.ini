[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v
# Default timeout for integration tests (overridden per-test where needed)
timeout = 30
markers =
    integration: marks tests as integration (require OpenEMR, skip in CI)
